{
  "name": "network-navigator-scripts",
  "private": true,
  "type": "module",
  "description": "Repository-level scripts (run from project root via `node scripts/<name>.ts`). Node's native TS strip-only mode handles execution; no install step required.",
  "scripts": {
    "test": "node --test \"*.test.ts\""
  }
}
